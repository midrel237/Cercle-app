import React from 'react';
import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function Screen() {
  return (
    <ScreenPlaceholder
      screenNumber={18}
      title="Biométrie"
      description="Activation empreinte / Face ID."
    />
  );
}
