import React from 'react';
import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function Screen() {
  return (
    <ScreenPlaceholder
      screenNumber={19}
      title="Notifications"
      description="Autorisation des notifications push."
    />
  );
}
