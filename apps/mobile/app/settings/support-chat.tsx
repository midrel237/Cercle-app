import React from 'react';
import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function Screen() {
  return (
    <ScreenPlaceholder
      screenNumber={66}
      title="Chat support"
      description="Discussion avec le support."
    />
  );
}
