import React from 'react';
import { ScreenPlaceholder } from '../../../src/components/ScreenPlaceholder';

export default function Screen() {
  return (
    <ScreenPlaceholder
      screenNumber={26}
      title="Règles du groupe (admin)"
      description="Pénalités, ordre de rotation."
    />
  );
}
