import React from 'react';
import { ScreenPlaceholder } from '../../../src/components/ScreenPlaceholder';

export default function Screen() {
  return (
    <ScreenPlaceholder
      screenNumber={41}
      title="Ajouter un membre (admin)"
      description="Invitation d'un nouveau membre."
    />
  );
}
