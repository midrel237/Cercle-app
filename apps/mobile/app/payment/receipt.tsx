import React from 'react';
import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function Screen() {
  return (
    <ScreenPlaceholder
      screenNumber={38}
      title="Reçu de paiement"
      description="Confirmation et reçu."
    />
  );
}
