import React from 'react';
import { ScreenPlaceholder } from '../../src/components/ScreenPlaceholder';

export default function Screen() {
  return (
    <ScreenPlaceholder
      screenNumber={46}
      title="Mes prêts"
      description="Vue d'ensemble des prêts internes."
    />
  );
}
