import React from 'react';
import { ScreenPlaceholder } from '../src/components/ScreenPlaceholder';

export default function Screen() {
  return (
    <ScreenPlaceholder
      screenNumber={58}
      title="Export du relevé"
      description="Export PDF du relevé de compte du groupe."
    />
  );
}
